<UserControl x:Class="SPAControls.GasLamp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SPAControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Border BorderThickness="2" BorderBrush="Black">
        <Grid x:Name="lampGrid" Background="{Binding SPABackgroud}">
            <Label Content='{Binding Path=Title, FallbackValue="Параметр"}' 
                   FontSize='{Binding Path=SPAFontSize}'
                   Foreground="#FF101010" 
                   Padding="2" 
                   FontWeight="Bold"
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
